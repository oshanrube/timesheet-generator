/*TODO*/
* create a db with following tables
	projects:
		id,name,description,start_date,end_date,total_hours
	work_log:
		id,project_id,start_time,end_time,comment,total_hours
* intergrate ajax submit for forms
* create a new connection class
* ping the server every two seconds
	if the prev record has the same comment and between 20 seconds
		it will update the last record
* add a start work and end work button
